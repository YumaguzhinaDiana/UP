<Window x:Class="ShoesShop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoesShop"
        mc:Ignorable="d"
        Title="Вход" Height="450" Width="800" Icon="/Resources/Icon.ico">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Background="#7FFF00">
            <Image x:Name="logo_img" Width="70" Height="70" Source="/Resources/Icon.png" HorizontalAlignment="Left"/>
            <TextBlock Text="ООО &quot;Обувь&quot; " FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button x:Name="catalog_btn" Content="Каталог" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#00FA9A" Click="catalog_btn_Click"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center" Grid.Column="1">
                <Label Content="Логин"/>
                <TextBox x:Name="login_tbx"/>
                <Label Content="Пароль"/>
                <TextBox x:Name="password_tbx" />
                <Button x:Name="enter_btn" Content="Войти" Background="#00FA9A" Margin="0,10" Click="enter_btn_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
